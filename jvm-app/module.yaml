product: jvm/app

dependencies:
  - ../shared
  - $compose.desktop.currentOs
  - $compose.components.resources

settings:
  compose: enabled
  jvm:
    jdk:
#      version: 25
      selectionMode: javaHome
